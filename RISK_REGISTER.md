# Risk Register — TABL-611

Maintained per TABL-611 (Risk Register - Monitor & Escalate Blockers). Reviewed at each standup; new risks added as identified, mitigation status updated as work lands. Last updated: 2026-08-05.

## Escalate now (Critical / High severity, active blockers)

| ID | Description | Likelihood | Impact | Risk Level | Owner |
|----|---|---|---|---|---|
| R-26 | `deploy-staging.yml`'s `deploy` job (Firebase Hosting, `projectId: tabl-app-staging`) and `api-gateway`'s separate Cloud Build continuous-deploy trigger both target GCP/Firebase project `tabl-app-staging`, which was deleted entirely on 2026-08-01 (along with `tabl-app-prod`) when the team closed GCP billing. **Confirmed failing in a live run**, not just predicted: promoting `integrate`→`develop` via PR #120 triggered `deploy-staging.yml` — `build-web`/`build-backend` passed, `deploy` failed at "Deploy to Firebase Hosting" with `FirebaseError: ... Project tabl-app-staging has been deleted.` (403, [run 30995772392](https://github.com/chukwuemekanwoke-jpg/comp47360-team2/actions/runs/30995772392), 2026-08-05T10:03). `api-gateway`'s Cloud Run auto-deploy didn't fire either, since that trigger no longer exists (not directly observable from Actions, but the underlying Cloud Build trigger and Cloud Run service are both gone per the 2026-08-01 project deletion). | Confirmed — already happened | High — staging deploy pipeline is fully non-functional, no cloud target exists for either service | Critical | Documented in `docs/deployment-guide.md` and `cloud_deployments/README.md` (local Docker run instructions added as the interim path). No infra or workflow fix yet — needs a decision: (a) stand up new GCP/Firebase infra from scratch and repoint the workflow + rebuild the Cloud Build trigger, or (b) explicitly retire the Firebase deploy step and rely on local Docker only | chukwuemekanwoke-jpg | Open — confirmed failing 2026-08-05, no mitigation started |
| R-21 | Two governance gaps surfaced together 2026-07-21: (1) PR #64 targets `main` directly from an external contributor (Rui), unreviewed, bypassing the `integrate`→`develop`→`main` promotion flow — same shape as the R-01 incident; content itself is low-risk (7 files, all `ml-pipeline/notebooks/*`, no application code, CI green, no merge conflicts — 4 of the 6 added files are already byte-identical in `integrate`, only `model training and evaluation_02.ipynb` is genuinely new there). (2) Separately, two commits (`d928c3b`, `17c843e`) landed as **direct pushes to `integrate`** — not PRs — adding a new ML training notebook + large xlsx data files, breaking the "every merge goes through a reviewed PR" pattern this repo has followed since PR #50. | Medium | Medium — no functional breakage yet, but erodes the review process this repo otherwise follows consistently | Medium | PR #64 can no longer merge unreviewed now that R-01's branch protection is live; still need to decide PR #64's actual fate (merge to `main` as-is, retarget to `integrate` first, or close) | chukwuemekanwoke-jpg | Open — protection landed, decision on PR #64 still pending |
| R-14 | **No verified fast-rollback mechanism.** TABL-507 required a <2 min rollback SLA; the only currently-available path (git revert + push + CI + redeploy) cannot hit that target (`ci.yml` alone runs 5 jobs, realistically 2-5 min). The fast path (Cloud Run revision traffic-shift) is undocumented and its availability depends on R-03 being resolved. Actual recovery time in a real incident is unknown. **Note (2026-08-05): the Cloud Run traffic-shift path this assumed no longer exists at all — Cloud Run itself was deleted along with the rest of GCP on 2026-08-01, see R-26.** | Medium | High — directly affects incident response time | High | chukwuemekanwoke-jpg |

## Full register

| ID | Description | Category | Likelihood | Impact | Risk Level | Mitigation | Owner | Status |
|----|---|---|---|---|---|---|---|---|
| R-26 | `deploy-staging.yml`'s Firebase Hosting deploy step and `api-gateway`'s Cloud Build trigger both target `tabl-app-staging`, deleted 2026-08-01 along with `tabl-app-prod` (GCP billing closed) — the next `develop` push will fail to deploy either service | Operational | High | High | **Critical** | Documented as broken in `docs/deployment-guide.md`; local Docker run path added in `cloud_deployments/README.md` as the interim workaround. No infra/workflow fix started | chukwuemekanwoke-jpg | Open — newly discovered 2026-08-05 |
| R-01 | No branch protection on `main` | Operational | High | High | **Critical** | Added root `CODEOWNERS` (PR #90, merged), then applied branch protection to `main`: require 1 approving review + code-owner review, `enforce_admins: false`, no force-push/delete | Chukwuemeka Nwoke | **Closed 2026-07-24** |
| R-25 | Auth rate limiter keys purely on IP (20 req/15min shared across login/register/forgot/reset); first went live in prod 2026-07-23T22:31, locks out mobile users sharing a carrier IP | Operational/Security | High | High | **Critical** | Fixed via `authRateLimitKey` (commit `96bac62`, PR #95 "auth-p0-hardening", 2026-07-25): keys by route + client IP + SHA-256 hash of email/token, so shared-IP users no longer share one budget. Verified live 2026-07-30: `96bac62` is an ancestor of `origin/develop`'s tip, and the deployed Cloud Run revision (`api-gateway-00029-jht`, image tag `00b11a3c...`) matches that exact commit | Chukwuemeka Nwoke | **Closed 2026-07-30** |
| R-02 | GCP deploy trigger status for api-gateway unverified from repo; `TASKS.md` claims `gcloud run deploy` steps exist in `deploy-staging.yml` but the file doesn't contain one | Operational | Medium | Medium-High | High | Cloud Run continuous-deploy trigger for api-gateway (Cloud Build, from `develop`) confirmed in GCP Console 2026-07-12; TASKS.md entry corrected same day | Chukwuemeka Nwoke | **Closed 2026-07-12** |
| R-03 | `ml-service` has no confirmed deploy mechanism at all | Operational | Medium-High | High | **Critical** | Cloud Build continuous-deploy trigger for ml-service (`rmgpgab-ml-service-...`, created 2026-07-08, branch `^develop$`) confirmed via `gcloud builds triggers list`/`describe` 2026-07-13 | Chukwuemeka Nwoke | **Closed 2026-07-13** |
| R-04 | `origin/TeslatotheMars-patch-5`: stale base, deletes `RestaurantSetupView.jsx`+tests and the fastapi-app `Dockerfile` (which we just fixed today), bundles an unrelated 100k-row Egyptian delivery dataset | Operational | Low-Medium | High | High | Do not merge until Rui confirms intent — asked in group chat 2026-07-11 | Rui Xu (clarify) / Chukwuemeka Nwoke (gatekeep) | Open — escalated, awaiting reply |
| R-05 | Duplicated busyness-prediction effort: our heuristic area_factor vs. Rui's already-trained OLS/RF/XGBoost model, both targeting the same problem | Strategic | Medium | Medium | Medium-High | Rui's trained XGBoost pipeline (`busyness_xgboost_pipeline.joblib`) is now the live model in `model_service.py`, loaded via `joblib.load`; `area_factor.py` is demoted to a feature-input helper (`resolve_taxi_zone`) only, not a competing predictor. Verified via PR #113/#114 ("busyness model orchestration") 2026-07-30 | Chukwuemeka Nwoke + Rui Xu | **Closed 2026-07-30** |
| R-06 | `tabl_app` Cloud SQL password is weak (`ChangeMe1!`) and was pasted in plaintext in earlier chat-history debugging | Security | Low | Medium | Medium | **Accepted, not rotated.** Owner decision 2026-07-30: app is demo-scoped and will be shut down, so rotation isn't worth doing | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted risk, not remediated** |
| R-07 | `enrich-places.js` blocked on Places API (New) being enabled + real spend approval — easy to forget since it needs manual GCP Console action | Financial/Operational | Medium | Low | Low | Places API (New) has been enabled. Owner decision 2026-07-30: the use case behind it is no longer active, so no further action needed | Chukwuemeka Nwoke | **Closed 2026-07-30** |
| R-08 | RevPASH: no schema, no route, no revenue data; depends on Yang Liu's unconfirmed SCRUM-302 status | Operational | High | Medium-High | High | Base schema/view/route built directly (migrations 006/007, `GET /revpash`, PR #52) without waiting on Yang Liu; this risk's premise no longer holds | Chukwuemeka Nwoke | **Closed 2026-07-21** (retroactively — base work actually shipped 2026-07-13, register just hadn't been corrected until R-19 investigation surfaced it) |
| R-09 | `rolling_busyness_7d` still never computed/fed to `/predict/busyness` despite `availability_snapshots` now being written — a silent quality gap, not an outage | Operational | High | Low-Medium | Medium | **Accepted, not built.** Reviewed PR #110/#111 directly — they wire `api-gateway`'s async busyness refresh (per-restaurant live score pulled from ml-service and written to `restaurants.busyness_score` + `availability_snapshots`), not a 7-day rolling feature. Confirmed `rolling_busyness_7d` remains an unused field in `main.py` ("not model features... neither used nor silently blended into the score") — the live XGBoost model's actual feature set (`MODEL_COLUMNS`) has no rolling term at all. Owner decision 2026-07-30: current model architecture doesn't need it, kept only for client-schema compatibility | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted, not needed by current model** |
| R-10 | `GET /restaurants/nearby` (diner-facing) still shows static busyness while the merchant detail view is live — inconsistent numbers across surfaces | Reputational | High | Medium | Medium | Fixed via PR #110/#111 ("async update of busyness with 1hr timeout"): `/nearby` now calls `refreshRestaurantBusyness` per stale restaurant (TTL-based) and serves the live `busyness_score` column directly, matching the merchant view. Verified in code 2026-07-30 | Chukwuemeka Nwoke | **Closed 2026-07-30** |
| R-11 | Local `main`/`develop` branches can silently drift far behind origin (found 32/35 commits behind on 2026-07-11) with no process to prevent recurrence | Operational | Medium-High | Low-Medium | Medium | **Accepted.** Owner decision 2026-07-30 to close as-is — informal `git fetch --all` habit stands as the only mitigation, no process change made | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted, not remediated** |
| R-12 | `TASKS.md` "Done" entries can be inaccurate relative to actual repo state (see R-02) — undermines trust in the tracker as a status source | Operational | Medium | Medium | Medium | **Accepted.** Owner decision 2026-07-30: `TASKS.md` is an internal-only document, so tracker drift isn't worth a standing process fix | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted, internal document** |
| R-13 | No confirmed Cloud SQL automated backup on `tabl-db-staging` | Security/Operational | Medium | High | **Critical** | Verified 2026-07-30 via Cloud SQL Admin API (`sql/v1beta4`, `settings.backupConfiguration`): backups `enabled: true`, 7 retained (COUNT-based retention), `pointInTimeRecoveryEnabled: true`, transaction log archiving on, 7-day log retention. No corresponding commit/change found in-repo — either already configured when this risk was logged and never re-checked, or set via GCP Console like R-02/R-03/R-18's blind spot | Chukwuemeka Nwoke | **Closed 2026-07-30** |
| R-14 | No verified fast-rollback mechanism; <2 min rollback SLA (TABL-507) not currently achievable via any confirmed path | Operational | Medium | High | High | Documented intended Cloud Run traffic-shift path in `docs/rollback-recovery-runbook.md`; R-03 (its blocker) is now closed, so this can be drilled and timed for real | Chukwuemeka Nwoke | Open — unblocked, not yet drilled |
| R-15 | Booking-maturity blend (added 2026-07-12) reads booking timestamps with `EXTRACT(HOUR ...)` in the **DB server's timezone** while the gateway buckets predictions by its own local clock — if Cloud SQL and Cloud Run run in different timezones (typical default: UTC vs UTC, but unverified), same-hour bucket counts silently shift by the offset | Operational | Medium | Low-Medium | Medium | Verified 2026-07-30: `tabl-db-staging` has no `timezone` database flag set (Cloud SQL Postgres defaults to UTC absent an override), and `api-gateway`'s Cloud Run service has no `TZ` env var (container defaults to UTC too). Both sides are UTC by convention, with nothing overriding either — no mismatch exists. Contrast with `getRevpash.js`/`getCampaignRevpashLift.js`, which explicitly convert to `America/New_York` where that's actually needed; the booking-maturity query correctly relies on the shared UTC default instead | Chukwuemeka Nwoke | **Closed 2026-07-30 — verified consistent, no fix needed** |
| R-16 | Onboarding busyness seeding makes `POST /restaurants` latency dependent on ml-service availability (adds stats query + ML round-trip inside the request); degradation is graceful (null → skip) but a slow-not-down ml-service delays merchant onboarding by up to `mlBusynessTimeoutMs` | Operational | Low-Medium | Low | Low | **Accepted as an expected performance limitation.** Owner decision 2026-07-30: the slowness traces to constrained hardware for this project, not a code defect — moving to fire-and-forget isn't planned | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted risk, not remediated** |
| R-17 | `api-gateway`'s Cloud Run secrets (`DATABASE_URL`, `JWT_SECRET`, `GOOGLE_MAPS_API_KEY`) are wired to Secret Manager's mutable `latest` alias rather than pinned versions; disabling any version bumps `latest` to something Cloud Run can't inject, aborting all new instance startups | Operational/Security | Medium | High | Medium (accepted) | `DATABASE_URL` re-pinned to explicit version 6 (2026-07-13, revision `api-gateway-00015-bzn`). `JWT_SECRET` and `GOOGLE_MAPS_API_KEY` remain on `latest` (reconfirmed live 2026-07-30) — **explicit owner decision 2026-07-30 to accept this exposure rather than pin further**, against the mitigation this register had been recommending. No technical change made; residual risk of a repeat R-17-style outage on either secret stands | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted risk, not remediated** |
| R-20 | `api-gateway`'s Cloud Run env var for the Google Maps key was bound under the wrong name (`ROUTES_API_SECRET` instead of the `GOOGLE_MAPS_API_KEY` the code actually reads in `config.js`). Secret existed and was correctly attached, just under a name nothing read — so `config.googleMapsApiKey` was `null` in production and every ETA/distance call silently fell back to the local haversine estimate (`source: "estimate"`) instead of the real Google Routes API (`source: "google"`), since this trigger was created 2026-07-08. No error, no outage — a quietly degraded feature. | Operational | Medium (config drift from GCP-console setup, not repo-driven — same blind spot as R-02/R-03) | Medium — ETA accuracy silently degraded, not availability | Medium | Fixed via `gcloud run services update api-gateway --remove-secrets=ROUTES_API_SECRET --update-secrets=GOOGLE_MAPS_API_KEY=GOOGLE_MAPS_API_KEY:latest` (2026-07-13, revision `api-gateway-00016-wcx`) | Chukwuemeka Nwoke | **Closed 2026-07-13** |
| R-18 | `RestaurantLocationPicker.jsx` (merged via PR #57) calls `GET /config/maps-key` to fetch the Google Maps browser key at runtime — this route doesn't exist on the backend. Degrades gracefully to manual lat/lng entry on the 404, so not user-blocking, but the map/autocomplete UX is dead until implemented. | Operational | High (route confirmed absent) | Low — graceful fallback exists | Low-Medium | Route implemented (PR #63), new browser-restricted key created + stored in Secret Manager as `MAPS_JS_API_KEY` (pinned to explicit version 1, not `latest`), bound to `api-gateway`. Promoted to `develop` (PR #65) and auto-deployed. Verified live 2026-07-21: `GET /config/maps-key` returns the real key, and `tabl-app-staging.web.app/register/restaurant` renders the real map picker (not the manual-entry fallback) | Chukwuemeka Nwoke | **Closed 2026-07-21** |
| R-19 | `CampaignHistory.jsx`'s new RevPASH lift badge (merged via PR #57, SCRUM-309/TABL-215) calls `GET /restaurants/:id/campaigns/:campaignId/revpash-lift` — route doesn't exist, same root blocker as R-08 (RevPASH backend owned by Yang Liu). Degrades gracefully ("pending backend support (TABL-118)"), not user-blocking. | Operational | High (route confirmed absent) | Low — graceful fallback exists | Low-Medium | Implemented (PR #70): dealRevpash from campaign-linked bookings over the campaign's active window; organicRevpash from same-hour non-campaign bookings over a trailing 30-day baseline; liftPercent=0 when no baseline exists; offPeak vs. restaurant's median hourly revpash. No methodology spec existed anywhere in docs, so this PR defines and documents one (README/openapi/api-contract-v0.md) | Chukwuemeka Nwoke | **Closed 2026-07-21** |
| R-22 | Every Tailwind color-opacity modifier (`bg-table-primary/10`, `border-table-danger/30`, `shadow-table-primary/25`, etc.) on the `table-*` theme palette silently resolves to fully transparent — confirmed via computed-style checks (`getComputedStyle` returns `rgba(0, 0, 0, 0)`), not a hunch. Root cause: `tailwind.config.js` defines these colors as raw `var(--table-primary, #31D5D5)` strings; Tailwind's opacity-modifier pipeline needs an unresolved `<alpha-value>` template (e.g. `rgb(49 213 213 / <alpha-value>)`) to inject an alpha channel, and silently drops the utility when it can't. Plain (non-opacity) color classes like `text-table-danger` are unaffected. This has been live since these tokens were introduced — every "tinted glass" status pill, alert-box background/border, and active-toggle tint across `BookingsList.jsx`, `CampaignHistory.jsx`, `LiveOfferTracker.jsx`, `AccessibilityPanel.jsx`, and the merchant dashboard's error banners has been rendering as fully invisible background/border (text/labels still show, since those use non-opacity classes) instead of the intended tint — a quietly degraded feature, no error, no outage, same shape as R-18/R-19/R-20. | Frontend/Operational | High (confirmed, currently live, spans most of the dashboard's status/alert UI) | Low-Medium — cosmetic only; text and functionality remain correct, just missing the intended tint/framing | Medium | Fixed app-wide (see Mitigated/Closed). | Chukwuemeka Nwoke | **Closed 2026-07-23** |
| R-23 | `backend/api-gateway/package-lock.json` is a **separate, standalone lockfile** consumed only by Cloud Build's isolated Docker context (build context scoped to `backend/api-gateway/` alone — this npm-workspaces monorepo's root `package-lock.json` isn't visible to it). Commit `c727d71` (2026-07-21, "Add rate limiting on auth and sensitive write routes") added `express-rate-limit` to `package.json` and correctly updated the *root* lockfile, but never regenerated this standalone copy. Result: every `api-gateway` Cloud Build deploy since has failed at `npm ci --omit=dev` (`Missing: express-rate-limit@8.6.0 from lock file`) — confirmed via Cloud Build history, last success was 2026-07-21T13:07, every attempt after (including two from this session's own promotions, PRs #84 and #86) failed identically. CI and local `npm ci` both stayed green the whole time because they resolve through the workspace root, which was never broken — only the isolated standalone copy was. Practical impact: the rate-limiting middleware (a security control) had never actually reached the deployed staging `api-gateway`; it silently ran a ~2-day-stale image the entire time, with zero errors visible anywhere in CI or the app itself. | Operational/Security | High (confirmed via Cloud Build API — 4 consecutive failures found) | High — a security control (rate limiting) was silently absent from the deployed service for ~2 days, and any other backend change in that window would have been equally stuck | High | Fixed (PR #87): regenerated the standalone lockfile by copying just `package.json` into a directory with no parent workspace context and running `npm install --package-lock-only`, verified `npm ci --omit=dev` succeeds in that same isolation. Promoted through `integrate`→`develop` (PR #88); confirmed live via the Cloud Build API (build succeeded), the Cloud Run API (`latestReadyRevision` now `api-gateway-00023-4mv`, image tag matching the new commit), and a direct `GET /api/v1/status` against the deployed URL (200, `database: connected`). | Chukwuemeka Nwoke | **Closed 2026-07-23** |
| R-24 | Firebase Hosting serves `index.html` with `Cache-Control: max-age=3600` by default — confirmed via response headers on the live staging URL. `firebase.json` has no `headers` override, so every deploy is followed by up to an hour where a browser (or intermediate cache) that already fetched the page can keep serving a stale `index.html`, which points at old content-hashed JS/CSS bundle URLs from a previous build. The hashed bundles themselves are safe to cache indefinitely (filenames change per build via Vite); only the unhashed HTML entry point is the problem. Surfaced when a login attempt right after a deploy still showed the pre-restyle page — confirmed the *server* content was already fully current (the live JS bundle contains the new `BrandMark` SVG path and "Secure Access" copy, zero occurrences of the old "Secure User Access" text) — purely a client/edge caching artifact, not a bad deploy. | Operational/DX | High — happens on every single deploy by design, not an edge case | Low-Medium — causes "did my deploy actually work" confusion and wasted debugging time, no data or security impact | Medium | **Accepted, not fixed.** Owner decision 2026-07-30: no intent to adjust Firebase Hosting's cache config. Workaround remains hard-refresh/incognito after a staging deploy if this bites again | Chukwuemeka Nwoke | **Closed 2026-07-30 — accepted risk, not remediated** |

## Mitigated / Closed

| ID | Description | Closed | Resolution |
|----|---|---|---|
| R-12 | `TASKS.md` "Done" entries can drift from actual repo state, undermining trust in the tracker | 2026-07-30 | **Accepted.** Owner decision: `TASKS.md` is internal-only, so tracker-accuracy drift isn't worth a standing process fix. |
| R-24 | Firebase Hosting's default `Cache-Control: max-age=3600` on `index.html` causes up to an hour of stale-page confusion after every deploy | 2026-07-30 | **Accepted, not fixed.** Owner decision: no intent to adjust the Firebase Hosting cache config. Hard-refresh/incognito remains the workaround if this causes confusion again. |
| R-05 | Duplicated busyness-prediction effort: heuristic `area_factor` vs. Rui's trained OLS/RF/XGBoost model | 2026-07-30 | Verified PR #113/#114 ("busyness model orchestration", merged 2026-07-27): `model_service.py` loads `busyness_xgboost_pipeline.joblib` as the actual live model; `area_factor.py` is now only a feature-input helper (`resolve_taxi_zone`) feeding the trained model, not a parallel heuristic predictor. Rui's model is what's actually scoring busyness in production. |
| R-06 | Weak staging Cloud SQL password (`ChangeMe1!`), previously pasted in plaintext during debugging | 2026-07-30 | **Accepted, not rotated.** Owner decision: the app is demo-scoped and will be shut down, so the rotation work isn't worth doing. |
| R-07 | `enrich-places.js` blocked on Places API (New) enablement + spend approval | 2026-07-30 | Places API (New) enabled. Owner decision: the use case behind this work is no longer active, closing without further action. |
| R-09 | `rolling_busyness_7d` never computed/fed into `/predict/busyness` | 2026-07-30 | **Accepted, not built.** Reviewed PR #110/#111 directly at the owner's request — they implement `api-gateway`'s async busyness refresh (live per-restaurant score pulled from ml-service into `restaurants.busyness_score`/`availability_snapshots`), which is a different mechanism than a 7-day rolling feature. `rolling_busyness_7d` remains an explicitly unused field in `ml-pipeline/fastapi-app/main.py`, and the live XGBoost model's actual feature set has no rolling term. Owner decision: current model architecture doesn't need it — field kept only for client-schema compatibility. |
| R-10 | `GET /restaurants/nearby` showed static busyness while the merchant view was live | 2026-07-30 | Fixed via PR #110/#111: `/nearby` now calls `refreshRestaurantBusyness` per stale restaurant (1hr TTL) and serves the live `busyness_score` directly — same source of truth as the merchant detail view. Verified in code, not just claimed. |
| R-11 | Local `main`/`develop` branches could silently drift far behind origin with no preventive process | 2026-07-30 | **Accepted.** Owner decision to close as-is; the informal `git fetch --all`-first habit remains the only mitigation. |
| R-15 | Booking-maturity SQL used `EXTRACT(HOUR ...)` in the DB server's timezone with no verification against the gateway's clock | 2026-07-30 | Verified via Cloud SQL Admin API and Cloud Run v2 API: neither service overrides its default timezone (no `timezone` DB flag, no `TZ` env var), so both run UTC by convention. No mismatch exists; the query didn't need an explicit `AT TIME ZONE` after all. |
| R-16 | Onboarding busyness seeding couples `POST /restaurants` latency to ml-service round-trip time | 2026-07-30 | **Accepted as an expected performance limitation**, not a defect — owner attributes the latency to constrained project hardware rather than the request-coupling design. No code change made. |
| R-17 | `JWT_SECRET` and `GOOGLE_MAPS_API_KEY` still bound to Secret Manager's mutable `latest` alias, exposed to the same failure mode that caused the 2026-07-13 outage (`DATABASE_URL` was pinned then; these two never were) | 2026-07-30 | **Accepted, not fixed.** Owner (chukwuemekanwoke-jpg) made an explicit decision 2026-07-30 to close this against the register's own recommendation (pin both to explicit versions) — no pinning was done, no other compensating control was added. Recorded here so the acceptance is auditable: if either secret's `latest` version is ever disabled the way `DATABASE_URL`'s was, the same full-outage failure mode is live again, by informed choice rather than oversight. |
| R-25 | Auth rate limiter (`authRateLimiter`) keyed purely on client IP — 20 req/15min shared across login/register/forgot/reset, locking out users behind a shared carrier IP | 2026-07-30 | Fixed via `authRateLimitKey` (commit `96bac62`, PR #95 "auth-p0-hardening", 2026-07-25): key is now route + client IP + SHA-256 hash of the request's email/token, so a shared IP no longer shares one budget across unrelated accounts. Verified genuinely live (not just merged) on 2026-07-30: confirmed `96bac62` is an ancestor of `origin/develop`'s current tip, and cross-checked that tip's commit hash against the deployed Cloud Run revision's image tag (`api-gateway-00029-jht`, image `...00b11a3c...`, updated 2026-07-27) — exact match. |
| R-13 | No confirmed Cloud SQL automated backup configuration on `tabl-db-staging` | 2026-07-30 | Queried the Cloud SQL Admin API directly (`GET .../sql/v1beta4/.../instances/tabl-db-staging`, `settings.backupConfiguration`): backups enabled, 7 retained (count-based), point-in-time recovery on, transaction log archiving on with 7-day retention. No repo commit explains when/how this was configured, so most likely set via GCP Console outside the repo (same blind spot as R-02/R-03/R-18/R-20) rather than newly fixed this session — closing on verified current state, not on remediation work done here. |
| R-23 | `backend/api-gateway`'s standalone (Docker-context-only) `package-lock.json` was out of sync with `package.json` since 2026-07-21, breaking every Cloud Build deploy of `api-gateway` — the deployed staging service silently ran a ~2-day-stale image the whole time | 2026-07-23 | Regenerated the standalone lockfile in isolation (`npm install --package-lock-only` against just `package.json`, no parent workspace context), verified `npm ci --omit=dev` succeeds in that same isolation (PR #87), promoted to `develop` (PR #88). Confirmed live via three independent checks: Cloud Build API shows the build succeeded, Cloud Run API shows `latestReadyRevision` is the new revision with the new commit's image tag, and a direct `GET /api/v1/status` against the deployed URL returns 200 with `database: connected`. |
| R-22 | Every Tailwind color-opacity modifier on the `table-*` theme palette silently resolved to fully transparent (`bg-table-primary/10`, `shadow-table-danger/30`, etc.) | 2026-07-23 | Added RGB-triple CSS custom properties (`--table-primary-rgb: 49 213 213`, one per token, for both the dark `:root` and `[data-theme='light']` blocks in `index.css`) alongside the existing hex vars, then repointed every `table.*` entry in `tailwind.config.js` at `rgb(var(--table-x-rgb, ...) / <alpha-value>)` so Tailwind can substitute the opacity modifier. Verified via computed-style checks in both themes (`bg-table-primary/10` → `rgba(49, 213, 213, 0.1)` dark, `rgba(8, 145, 178, 0.1)` light) and confirmed the fix cascades with zero code changes to pages never touched this session (`ForgotPasswordView.jsx`'s pre-existing `shadow-lg shadow-table-primary/10` now resolves correctly). Also converted the two inline `color-mix()` box-shadow workarounds added for R-22 (login + flash-deal buttons) back to plain `shadow-lg shadow-table-{color}/40` now that the utility works, so the workaround doesn't linger once its cause is gone. Full lint + 43/43 tests + production build all clean. |
| R-18 | `RestaurantLocationPicker.jsx` map picker dead — `GET /config/maps-key` route didn't exist | 2026-07-21 | Implemented the route (PR #63, `config.mapsJsApiKey`/`MAPS_JS_API_KEY`, 404 when unset). Created a new browser-restricted key (Maps JS + Places API only, HTTP-referrer-locked to `tabl-app-staging.web.app` + localhost dev) — deliberately separate from the server-side `googleMapsApiKey`, since a referrer restriction and server-to-server calls are mutually exclusive on one key. Stored as Secret Manager secret `MAPS_JS_API_KEY`, bound to `api-gateway` pinned to explicit version `1` (not `latest`, per the R-17 lesson). Promoted `integrate`→`develop` (PR #65); `api-gateway` auto-deployed. Verified live: endpoint returns the real key, and the registration page renders the actual map/autocomplete picker instead of the manual-entry fallback. |
| R-19 | `CampaignHistory.jsx`'s RevPASH lift badge dead — `GET /campaigns/:id/revpash-lift` route didn't exist | 2026-07-21 | Implemented (PR #70). Defined and documented a lift methodology from scratch (none existed anywhere in docs): dealRevpash from this campaign's bookings over its active window; organicRevpash from same-hour-of-day non-campaign bookings over a trailing 30-day baseline (not the campaign's own window, which usually has little organic activity by definition); liftPercent=0 instead of dividing by zero when no baseline exists; offPeak vs. the restaurant's median hourly revpash. 5 new tests (unit + route-level), OpenAPI/drift checks clean. |
| R-08 | RevPASH had zero backend progress, blocked on Yang Liu's unconfirmed SCRUM-302 status | 2026-07-13 (discovered stale 2026-07-21) | Actually resolved back on 2026-07-13 — migrations 006/007 and `GET /revpash` shipped via PR #52, built directly rather than waiting on Yang Liu. The register just never got corrected, so R-19 inherited a stale "blocked on Yang Liu" premise for over a week until the R-19 investigation caught it. Same failure mode as R-12 (stale tracker entries eroding trust) — worth a periodic spot-check habit, not just a one-off fix. |
| R-20 | `api-gateway`'s Cloud Run secret for the Google Maps key bound under the wrong env var name (`ROUTES_API_SECRET`), so ETA calls silently always used the haversine fallback instead of the real Routes API | 2026-07-13 | Confirmed via `gcloud run services describe` that the secret existed and was attached, just under a name `config.js` never reads. Fixed with `gcloud run services update api-gateway --remove-secrets=ROUTES_API_SECRET --update-secrets=GOOGLE_MAPS_API_KEY=GOOGLE_MAPS_API_KEY:latest` (revision `api-gateway-00016-wcx`). Verified live: `GET /api/v1/restaurants/:id/eta` now returns `"source":"google"` (was `"estimate"`). `JWT_SECRET`/`DATABASE_URL` untouched. Confirmed via the Cloud Build trigger's build config that deploys only patch the image, never env vars/secrets — so this fix persists across future `develop` auto-deploys. |
| R-02 | api-gateway GCP deploy trigger unverified from repo / TASKS.md claim mismatch | 2026-07-12 | Cloud Run continuous-deploy trigger (Cloud Build, from `develop`) confirmed in GCP Console; `deploy-staging.yml` comment documents the mechanism; TASKS.md Done entry corrected. Note: this confirmation covers **api-gateway only** — R-03 (ml-service deploy path) remains open and still gates R-14. |
| R-03 | ml-service (fastapi-app) deploy mechanism unconfirmed from repo search | 2026-07-13 | Same root cause as R-02: the trigger is GCP-console-configured, not repo YAML, so a repo-only search missed it. `gcloud builds triggers list` confirms `rmgpgab-ml-service-europe-west1-...` exists, is enabled, targets branch `^develop$`, and has been live since 2026-07-08 — it was already deploying ml-service the whole time this risk was open. Unblocks R-14. |

## Standup log

- **2026-08-05 (R-26 opened; deployment-guide reviewed)**: Asked to review `docs/deployment-guide.md` "in light of recent developments." Found it was last edited 2026-06-24 and stale on two levels: (1) it described `deploy-staging.yml`'s `deploy` job as a still-unwired placeholder, when it had actually been a real Firebase Hosting deploy since 2026-07-09 (commit `b8a37cd`); (2) more urgently, that real deploy step — and `api-gateway`'s separate Cloud Build continuous-deploy trigger — both target `tabl-app-staging`, which was deleted along with `tabl-app-prod` on 2026-08-01 (see project memory: gcp-tabl-app-deployment). No push has hit `develop` since 2026-07-27, so the break hasn't surfaced as an observed failure yet, but the next `integrate`→`develop` promotion will hit it. Logged as **R-26** (Critical, Escalate now) and cross-referenced from R-14 (whose assumed Cloud Run rollback path is now also gone). Updated `docs/deployment-guide.md` (staging/production deploy sections, troubleshooting table, new infra-status callout at the top) and `cloud_deployments/README.md` (added local Docker build/run instructions as the only currently-working way to run the app, and corrected its "GCP is where the app actually runs today" claim from the previous session).
- **2026-07-11**: Register created from findings surfaced during busyness-prediction wiring work and branch review. 3 items flagged for escalation (R-01, R-03, R-08). Awaiting: Rui's reply (R-04, R-05), Yang Liu's status (R-08), GCP Console verification (R-02, R-03).
- **2026-07-11 (TABL-507)**: Wrote `docs/rollback-recovery-runbook.md`. Confirmed `git revert` itself is fast (~200ms, tested on a scratch branch) but found two new Critical/High risks in the process: no confirmed DB backup (R-13) and no verified fast-rollback path to actually hit the ticket's <2 min target (R-14). Both trace back to the same unresolved GCP Console verification as R-02/R-03 — escalating as one bundled ask rather than four separate ones.
- **2026-07-12 (R-08 checked, R-05 movement)**: R-08 re-verified against the repo — still zero RevPASH backend progress (no migration 006, no view, no route) and Yang Liu's only activity since 2026-07-09 is PR merges. Escalation trigger fires at Tuesday's standup as planned. Separately, Rui pushed his final busyness model today to `TeslatotheMars-patch-1` (notebook + description + training data), fulfilling his 2026-07-09 email commitment — R-05 moves from "waiting on reply" to "sync on integration." Caution: patch-1 was previously listed as a redundant branch to close without merging; that's now stale — it carries real new work and must be reviewed, not closed.
- **2026-07-12 (R-02 closed)**: Cloud Run continuous-deploy trigger for api-gateway confirmed directly in the GCP Console (Cloud Build, deploying from `develop` — matching the comment in `deploy-staging.yml`'s `build-backend` job). First register closure. TASKS.md's inaccurate "gcloud run deploy steps in deploy-staging.yml" Done entry corrected (R-12's known instance). Scope note: api-gateway only — the ml-service trigger (R-03) was NOT part of this confirmation and stays Critical/escalated.
- **2026-07-12**: Implemented merchant busyness maturation — new venues score from location prior (taxi + pedestrian) at onboarding, then blend toward their own confirmed bookings over 30 days (capped at 60% booking weight, shrinkage-damped). Two new risks logged: timezone consistency of the hour-bucket SQL (R-15, needs verification) and onboarding latency coupling to ml-service (R-16, accepted). Note R-05 (duplicated model effort with Rui) is still open — the maturation layer is model-agnostic (blends whatever the prior produces), so it survives a swap to Rui's trained model.
- **2026-07-13 (R-03 closed)**: While reviewing the `integrate` branch and its promotion path to `develop`, ran `gcloud builds triggers list` directly against `tabl-app-staging` and found `rmgpgab-ml-service-europe-west1-...` already exists, is enabled, and has targeted branch `^develop$` since 2026-07-08 — before this risk was even logged (2026-07-11). Same blind spot as R-02: these continuous-deploy triggers live in GCP Console config, not repo YAML, so the original "no visible trigger config anywhere in the repo" finding was accurate about the repo but wrong about the actual deploy mechanism. Closing R-03 and unblocking R-14 as a result.
- **2026-07-13 (R-17: staging outage from a disabled secret)**: Deployed staging site started 500-ing entirely (account creation was just the first symptom reported). Traced to `DATABASE_URL` secret version 7 — created earlier today by an unidentified party, then disabled — becoming the `latest` alias that Cloud Run's `api-gateway` pins to; Cloud Run cannot inject a disabled secret version, so every new instance failed to start as old ones autoscaled out. Fixed by re-pinning `DATABASE_URL` to explicit version 6 (known-good, used successfully all session for local Cloud SQL connector work); verified with a live registration request against the real staging URL (201 Created). Logged as R-17 rather than folding into R-06 (weak password) because the failure mode is structural — any Cloud Run secret pinned to `latest` is one `disable` action away from a full outage — and two more secrets (`JWT_SECRET`, `ROUTES_API_SECRET`) are still exposed to it. Who disabled version 7, and why, is still unknown.
- **2026-07-13 (R-20 closed: Routes API key silently unused in production)**: While investigating R-18 (missing `/config/maps-key` route), checked whether onboarding referenced the Maps key already stored in Secret Manager. Found the secret (`GOOGLE_MAPS_API_KEY`, created 2026-07-08) was correctly attached to `api-gateway`'s Cloud Run service — but bound under env var name `ROUTES_API_SECRET`, which nothing in the codebase reads (`config.js` only reads `process.env.GOOGLE_MAPS_API_KEY`). Result: every ETA calculation had been silently falling back to the local haversine estimate since this was deployed, with no error surfaced anywhere. Fixed by rebinding the secret under the correct name via `gcloud run services update` (revision `api-gateway-00016-wcx`); verified live that `/api/v1/restaurants/:id/eta` now returns `"source":"google"` instead of `"source":"estimate"`. Confirmed the fix will survive future deploys since the Cloud Build trigger's deploy step only patches the container image, never env vars/secrets.
- **2026-07-13 (PR #57 merged: frontend-integration-v2)**: Reviewed and merged Andrew Mitchell's `feature/frontend-integration-v2` into `integrate` (even with `integrate` already, no rebase needed). Brings password-reset frontend + shared `PasswordInput` component (backend support already on `integrate` via PR #55, so fully live now), a real server-side logout (`POST /auth/logout` + RTK Query cache reset), a `noValidate` fix for the campaign form's native-vs-styled validation conflict, plus two features built ahead of their backend: the RevPASH lift badge (`CampaignHistory.jsx`) and the Google Maps location picker (`RestaurantLocationPicker.jsx`). Both new frontend features degrade gracefully on 404 rather than breaking, but neither is functionally live yet — logged as new risks R-18 (`GET /config/maps-key` missing) and R-19 (`GET /restaurants/:id/campaigns/:campaignId/revpash-lift` missing, blocked on the same R-08 RevPASH backend work). Also checked a suspected `minLength={8}` regression on `RegisterView.jsx` from swapping in `PasswordInput` — false alarm, the form already has `noValidate` so the HTML attribute was inert; `isValidPassword` (JS, min 8 chars) still enforces it before submit. Local `integrate` fast-forwarded to `047cbdd` after the merge.
- **2026-07-21 (R-18 closed; R-21 opened)**: Implemented `GET /config/maps-key` (PR #63), created a new browser-restricted Maps JS/Places key separate from the server-side Routes API one (referrer restriction and server-to-server calls can't share a key), stored it as Secret Manager secret `MAPS_JS_API_KEY` pinned to explicit version `1`, bound it to `api-gateway`, and promoted `integrate`→`develop` (PR #65) so the auto-deploy trigger picked it up. Verified live: the endpoint returns the real key and the registration page renders the actual map picker. Closing R-18. While investigating a separate ask (check PR #64), found it's an unreviewed PR from Rui targeting `main` directly — technically low-risk content (only `ml-pipeline/notebooks/*`, no app code, CI green) but bypasses the promotion flow entirely on a `main` with zero branch protection (R-01), and sits there mergeable by anyone. Also found two commits (`d928c3b`, `17c843e`) pushed directly to `integrate` without a PR, breaking the reviewed-merge pattern this repo has followed since PR #50. Logged both as R-21; no action taken pending an explicit decision. Also, mid-session: a `git checkout develop` failed due to an unrelated uncommitted edit on `feature/frontend-sprint-4-complete` (present since before this session), but a subsequent `git pull origin develop` still ran against that branch instead, creating an unwanted local-only merge commit — caught immediately, undone with `git reset --merge ORIG_HEAD` (uncommitted edit preserved throughout), then redone safely by stashing the file first. No data lost, nothing was pushed.
- **2026-07-21 (R-19 closed; R-08 closed retroactively)**: Investigated R-19's premise before starting — found R-08 ("RevPASH blocked on Yang Liu") was stale: the base schema/view/route actually shipped 2026-07-13 via PR #52, built directly rather than waiting on Yang Liu, but the register was never corrected. So R-19 wasn't blocked on anyone external, just unbuilt. No lift-comparison methodology existed anywhere in `docs/`, so defined one before implementing: dealRevpash from this campaign's bookings over its active window; organicRevpash from same-hour-of-day non-campaign bookings over a trailing 30-day baseline (deliberately not the campaign's own window, since that usually has little organic activity by definition — that's often why a deal ran then); liftPercent=0 rather than a divide-by-zero when no baseline exists yet; offPeak vs. the restaurant's median hourly revpash. Implemented as `GET /campaigns/:id/revpash-lift` (PR #70, 5 new tests, OpenAPI/drift checks clean), merged into `integrate`. Closing both R-19 and R-08 (the latter closed retroactively to 2026-07-13, correcting the stale entry rather than leaving it as a live confusion source — same category as R-12's "stale tracker" concern).
- **2026-07-21 (promotion in flight)**: Opened PR #71 (`integrate`→`develop`, CI green, 7 commits) to carry both this session's closures — R-18 (`GET /config/maps-key`, PR #63) and R-19 (`GET /campaigns/:id/revpash-lift`, PR #70) — through to the deployed staging site, plus PR #69's campaign-offers endpoint. Net position after today: two features that were shipped-but-silently-dead in production (map picker, RevPASH lift badge) are now real; one stale risk (R-08) got corrected instead of left to mislead the next person; R-20's misnamed-secret bug from last week stayed fixed. Still open and untouched: R-21 (PR #64 / direct-push governance gap, awaiting a decision) and R-01 (main branch protection, made more urgent by R-21). PR #71 itself not yet merged — holding per usual pattern until explicitly told to merge.
- **2026-07-22 (PR #71 merged; frontend sprint 4 promoted via PR #72)**: PR #71 merged, closing out the previous entry's "held for merge" note — R-18/R-19/campaign-offers are now live on `develop`. Separately, `feature/frontend-sprint-4-complete` (PR #67) merged into `integrate`: Discover tab (top picks, quiet spots, cuisine browsing), cuisine photos on restaurant cards, editable profile preferences, a collapsible filters panel, shared search/busyness filtering across card and map views, an Ionicons refactor (replacing emoji icons across bookings/offers/inbox), a light-mode contrast fix on settings switches, and a fix for the map not tracking real GPS location after mount (`initialRegion` only applies once at mount; the camera now animates to the real fix via a ref once it lands asynchronously, instead of staying stuck on the Times Square fallback). That batch promoted `integrate`→`develop` via PR #72 and is now live on staging. No new risks surfaced by this batch. Still open and untouched: R-21 (PR #64 / direct-push governance gap) and R-01 (main branch protection).
- **2026-07-23 (login restyle + dashboard page split shipped; R-22 opened)**: Two new branches, each reviewed and CI-green before merging: `feature/login-restyle` (PR #82) reskins the login page to the "Kinetic Operational" theme (glowing `BrandMark` icon, ambient grid/glow backdrop replacing the old stock photo, glow under the primary button); `feature/dashboard-page-split` (PR #83) splits the single `/merchant` page — which fired every RTK Query hook on mount regardless of what was visible — into routed `/merchant` (Overview), `/merchant/tables`, `/merchant/settings` pages, each only calling the hooks it needs, plus a sidebar-nav shell rebuild and a `UserBadge` (initials avatar + the signed-in manager's real `displayName` from `GET /users/me`, not a placeholder). Both merged to `integrate`, then promoted `integrate`→`develop` via PR #84 (CI green) and merged. Deliberately left out of both: any UI the backend doesn't actually support (per-table floor plan, table/server search, team-invite controls, a system-health panel) — the Stitch design references we worked from assumed those, but no schema/route exists for any of them. While verifying the button glow rendered, found and logged **R-22**: every Tailwind color-opacity modifier (`bg-table-primary/10`, `shadow-table-primary/25`, etc.) on the `table-*` palette silently resolves to fully transparent, confirmed via computed-style checks — root cause is `tailwind.config.js` defining these colors as raw `var()` strings rather than an opacity-templated `rgb(... / <alpha-value>)` format. This has been live since the tokens were introduced and affects most of the dashboard's existing tinted status pills/alert boxes/toggle states, not just this session's new code. Worked around locally with inline `color-mix()` styles in the new components; app-wide fix not yet done — logged rather than fixed, since it's a much larger, separate change than what was asked for this session. Also noted in passing, not registered as a risk: local `backend/api-gateway` node_modules is currently missing `express-rate-limit` (blocks `npm run dev` locally), but CI's "API gateway (install + test)" job installs and passes cleanly, so this is a stale local install, not a manifest/repo issue — `npm install --workspace=backend/api-gateway` resolves it.
- **2026-07-23 (R-22 closed)**: Fixed the opacity bug app-wide on its own branch rather than patching individual components. Added RGB-triple CSS custom properties (`--table-primary-rgb`, etc., one per existing token) to both theme blocks in `index.css`, alongside the existing hex vars, then repointed every `table.*` color in `tailwind.config.js` at `rgb(var(--table-x-rgb, ...) / <alpha-value>)`. Two files, zero component changes needed for the fix itself to cascade — verified this directly by checking `ForgotPasswordView.jsx`'s pre-existing (untouched this session) `shadow-lg shadow-table-primary/10` now resolves to a real tinted shadow, and confirmed both dark and light theme variants resolve to the correct RGB triple via computed-style checks. Also reverted the two `color-mix()` inline-style workarounds from earlier today (login + flash-deal button glows) back to plain `shadow-lg shadow-table-{color}/40`, since the workaround's reason no longer exists. Full lint, 43/43 tests, and a production build all clean.
- **2026-07-23 (R-23 found + closed same session; R-24 found)**: Asked to check GCP deploy config directly rather than assume from memory. `gcloud` CLI auth had expired (non-interactive re-auth not possible), but `gcloud auth application-default print-access-token` still worked, so queried the Cloud Build/Cloud Run REST APIs directly with that token instead. Found `api-gateway`'s Cloud Build trigger had been failing on every single push to `develop` since 2026-07-21T13:07 (4 consecutive failures, including this session's own PR #84 and #86 promotions) — `npm ci --omit=dev` failing on a standalone, Docker-context-only `package-lock.json` under `backend/api-gateway/` that never got regenerated when `express-rate-limit` was added to `package.json` (`c727d71`). This repo is an npm-workspaces monorepo; CI and local `npm ci` both resolve through the root lockfile (which was fine) and so never caught that the *isolated* copy Cloud Build actually uses was broken. Practical impact: rate-limiting middleware had silently never reached the deployed staging service. Fixed by regenerating the standalone lockfile in true isolation (copied just `package.json` into a directory with no parent workspace, `npm install --package-lock-only`, verified `npm ci --omit=dev` succeeds there), shipped via PR #87→`integrate`→PR #88→`develop`, and confirmed genuinely live — not just merged — via three independent checks: Cloud Build API (build succeeded), Cloud Run API (`latestReadyRevision` = new revision, image tag = new commit SHA), and a direct `GET /api/v1/status` against the live URL (200, database connected). Logged and closed as R-23 in the same session. Separately, while verifying the frontend also picked up the day's changes, found R-24: Firebase Hosting serves `index.html` with the platform default `Cache-Control: max-age=3600` (`firebase.json` has no `headers` override), so a browser that fetched the page before a deploy can keep serving a stale entry point — pointing at old hashed bundle URLs — for up to an hour after. Confirmed this was purely a caching artifact and not a bad deploy: the live JS bundle already contained the new `BrandMark` icon path and "Secure Access" copy, zero trace of the old text. Logged as R-24, left open (not yet fixed) — the real fix is a `firebase.json` `headers` override giving `index.html` a short/no-cache policy while leaving the content-hashed `/assets/**` files on their long default cache.
- **2026-07-30 (R-12 and R-24 closed by owner decision — full register now empty of open items)**: Owner closed the last two open risks: R-24 (Firebase Hosting's 1hr `index.html` cache) — explicit decision not to adjust the cache config, workaround stands if it bites again; R-12 (`TASKS.md` accuracy drift) — accepted since `TASKS.md` is internal-only and not worth a standing process fix. Combined with the same-day closures below (R-05 through R-17), every item in the Full register is now Closed — nothing open as of this update.
- **2026-07-30 (batch closure: R-05, R-10 verified fixed; R-06/R-07/R-09/R-11/R-16 accepted by owner decision; R-15 verified consistent; R-12/R-24 still open)**: Went through the remaining full-register items with the owner. **R-05**: confirmed Rui's trained XGBoost pipeline (`busyness_xgboost_pipeline.joblib`) is the live model in `model_service.py`; `area_factor.py` demoted to a feature helper only — closing as genuinely resolved (PR #113/#114). **R-10**: confirmed `/nearby` now serves live, TTL-refreshed `busyness_score` via PR #110/#111 — closing as resolved. **R-09**: owner initially believed this was also fixed by PR #110/#111 ("api-gateway triggers pulling busyness from ml-service and writing to DB"); reviewed both PR diffs directly and found that's true but describes a different mechanism — a live point-in-time score refresh, not a 7-day rolling average. `rolling_busyness_7d` is still an explicitly unused field in `main.py`, and the current model's feature set has no rolling term at all. Surfaced this distinction before closing; owner then made the call to close R-09 as accepted (model architecture doesn't need it) rather than leave it open chasing a feature the model will never use. **R-15**: checked at owner's request — no timezone override on either Cloud SQL (`tabl-db-staging`) or Cloud Run (`api-gateway`), so both default to UTC; no mismatch, closing as verified-consistent. **R-06, R-07, R-11, R-16**: closed on explicit owner risk-acceptance (demo app winding down, Places API use case ended, branch-drift habit accepted as sufficient, ml-service latency attributed to project hardware limits) — none of these involved a code or infra change. **R-12** was elaborated on request (it's about `TASKS.md` "Done" claims drifting from repo reality, first caught via R-02 and recurring via R-08) but left open pending an explicit decision. **R-24**: owner stated "it actually worked," but `firebase.json` still has no `headers` override and `index.html` still defaults to `max-age=3600` — flagged this contradiction and left R-24 open pending clarification rather than closing on an unverified claim.
- **2026-07-30 (R-17 closed by explicit owner decision, not remediation)**: After the re-verification pass below reconfirmed R-17 as the last open Critical (`JWT_SECRET`/`GOOGLE_MAPS_API_KEY` still on `latest`), owner (chukwuemekanwoke-jpg) made the call to close it as-is — accepting the exposure rather than pinning the remaining two secrets, against this register's own standing recommendation. No infrastructure change made. Logged explicitly as an accepted-risk closure so this reads as an informed decision later, not a dropped item: if either secret's `latest` version is ever disabled, the same failure mode that caused the 2026-07-13 outage recurs.
- **2026-07-30 (re-verification pass: R-13 and R-25 closed, R-17 reconfirmed open)**: Asked to re-check the three Critical risks against live state before standup, since the register was 6 days stale. `git fetch`'d first (per R-11's lesson) before drawing any conclusions from local branch refs. **R-25**: found the code already keys by IP+SHA-256(email/token) via `authRateLimitKey` — not IP alone as the register claimed. Traced the fix to commit `96bac62` (PR #95 "auth-p0-hardening", 2026-07-25, one day after this risk was logged). Confirmed it's not just merged but actually deployed: `96bac62` is an ancestor of `origin/develop`'s tip, and that exact commit hash appears in the deployed Cloud Run revision's image tag (`api-gateway-00029-jht`). Closing R-25. **R-13**: `gcloud sql instances describe` failed (expired interactive auth token, non-interactive re-auth impossible — same limitation noted in the 2026-07-23 R-23 entry), so queried the Cloud SQL Admin REST API directly with `gcloud auth application-default print-access-token` instead (first REST call 404'd on API version `v1`; `v1beta4` is correct). Backups are enabled: 7 retained, PITR on, transaction log archiving on. No repo history explains this configuration, so it was likely set in GCP Console and never re-checked after the initial "not yet checked" note — closing on verified state, not attributing it to session work. **R-17**: re-queried the Cloud Run v2 API's env bindings directly — `JWT_SECRET` and `GOOGLE_MAPS_API_KEY` are still on the `latest` secret alias; only `DATABASE_URL` (v6) and `MAPS_JS_API_KEY` (v1) are pinned. No change since 2026-07-13 — stays open and Critical, still the only unresolved item of the three.
- **2026-07-24 (mobile login incident: R-25 found; R-01 closed)**: Asked to check why the mobile app couldn't log in. Ruled out infra first: Cloud Run `api-gateway` healthy on `api-gateway-00023-4mv`, DB connected, `JWT_SECRET` correctly bound. Traced it to the auth rate limiter added alongside R-23's fix — it only started actually running in production the night before (2026-07-23T22:31, once R-23 unblocked deploys) and keys purely on client IP (20 req/15min shared across login/register/forgot/reset). Reproduced the exact 429 via direct requests, then got independent confirmation via a user-supplied screenshot of the real login page showing the identical `RATE_LIMITED` message. Logged as **R-25** (Critical) — mobile carriers' shared/CGNAT IPs make this far easier to exhaust than for web/wifi users. Fix (IP+email keying) not yet implemented; immediate relief (raising `RATE_LIMIT_AUTH_MAX` via Cloud Run env var) was proposed but the session pivoted to branch protection before it was applied — **R-25 remains open, no mitigation live yet.** Separately, was asked to promote `develop`→`main`; found `main` is 144 commits / 253 files behind. Given the scale and R-01 (zero branch protection on `main`), addressed R-01 first: added root `CODEOWNERS` (PR #90, merged) and applied branch protection (1 approving review + code-owner review required, `enforce_admins: false`, no force-push/delete) — closing R-01 and incidentally protecting PR #64 (R-21) from an unreviewed direct merge. Then opened PR #91 (`develop`→`main`), deliberately **not** merged — held for manual review given its size, unlike routine promotions.
