# Risk Register — TABL-611

Maintained per TABL-611 (Risk Register - Monitor & Escalate Blockers). Reviewed at each standup; new risks added as identified, mitigation status updated as work lands. Last updated: 2026-07-23.

## Escalate now (Critical / High severity, active blockers)

| ID | Description | Likelihood | Impact | Risk Level | Owner |
|----|---|---|---|---|---|
| R-18 | Cloud Run's `api-gateway` pins secrets to the mutable `latest` alias instead of explicit versions. Already caused a full staging outage (2026-07-13): an unknown party created and then disabled `DATABASE_URL` version 7, making `latest` unresolvable — Cloud Run refused to start any new instance, and the site 500'd entirely (not just registration) once old instances recycled out. `DATABASE_URL` is now pinned to version 6 (known-good), but `JWT_SECRET` and `GOOGLE_MAPS_API_KEY` are still on `latest` and remain exposed to the identical failure mode. | Medium (has already happened once) | High — full site outage | **Critical** | chukwuemekanwoke-jpg |
| R-14 | **No confirmed Cloud SQL backup configuration** for `tabl-db-staging` — found while writing TABL-507's rollback runbook. If staging data is lost or corrupted (bad migration, bad restore, accidental delete), there is currently nothing to recover from. | Medium | High — unrecoverable data loss if it happens | **Critical** | chukwuemekanwoke-jpg |
| R-02 | `main` still has **zero branch protection**. Already caused one incident (Yang Liu's PR #45 merged unreviewed, later reverted via PR #48). Next `develop`→`main` promotion is unprotected against a repeat. **Actively live**: PR #64 (`TeslatotheMars-patch-1` → `main` directly) has been sitting open, CI-green and mergeable, since 2026-07-17 — nothing technical stops it merging straight to production, bypassing `integrate`/`develop` entirely. | High (has already happened once, and a second instance is currently possible) | High (production branch) | **Critical** | chukwuemekanwoke-jpg |
| R-22 | Two governance gaps surfaced together 2026-07-21: (1) PR #64 targets `main` directly from an external contributor (Rui), unreviewed, bypassing the `integrate`→`develop`→`main` promotion flow — same shape as the R-02/R-01 incident; content itself is low-risk (7 files, all `ml-pipeline/notebooks/*`, no application code, CI green, no merge conflicts — 4 of the 6 added files are already byte-identical in `integrate`, only `model training and evaluation_02.ipynb` is genuinely new there). (2) Separately, two commits (`d928c3b`, `17c843e`) landed as **direct pushes to `integrate`** — not PRs — adding a new ML training notebook + large xlsx data files, breaking the "every merge goes through a reviewed PR" pattern this repo has followed since PR #50. | Medium | Medium — no functional breakage yet, but erodes the review process this repo otherwise follows consistently | Medium | Decide PR #64's fate (merge to `main` as-is, retarget to `integrate` first, or close); consider whether `main`'s branch protection (R-02) should land before any further direct-to-main PRs are reviewed | chukwuemekanwoke-jpg | Open — awaiting decision |
| R-01 | `main` is missing functionality present on `develop`/`integrate` (JWT auth, restaurant registration routes, migrations 004/005) because PR #48 reverted PR #45 on `main` only. Promoting `develop`→`main` without reconciling this could produce confusing conflicts or silently drop work. | Medium | High | High | chukwuemekanwoke-jpg |
| R-15 | **No verified fast-rollback mechanism.** TABL-507 required a <2 min rollback SLA; the only currently-available path (git revert + push + CI + redeploy) cannot hit that target (`ci.yml` alone runs 5 jobs, realistically 2-5 min). The fast path (Cloud Run revision traffic-shift) is undocumented and its availability depends on R-04 being resolved. Actual recovery time in a real incident is unknown. | Medium | High — directly affects incident response time | High | chukwuemekanwoke-jpg |

## Full register

| ID | Description | Category | Likelihood | Impact | Risk Level | Mitigation | Owner | Status |
|----|---|---|---|---|---|---|---|---|
| R-01 | `main` missing auth/registration/migration work reverted by PR #48, diverging from `develop`/`integrate` | Operational | Medium | High | High | Reconcile deliberately before next promotion — decide whether PR #45's content should be reintroduced via a fresh, reviewed PR | chukwuemekanwoke-jpg | Open — planned, not started |
| R-02 | No branch protection on `main` | Operational | High | High | **Critical** | CODEOWNERS (me only) + "require review from code owners," `enforce_admins` off — planned before next promotion | chukwuemekanwoke-jpg | Open — planned, not started |
| R-03 | GCP deploy trigger status for api-gateway unverified from repo; `TASKS.md` claims `gcloud run deploy` steps exist in `deploy-staging.yml` but the file doesn't contain one | Operational | Medium | Medium-High | High | Cloud Run continuous-deploy trigger for api-gateway (Cloud Build, from `develop`) confirmed in GCP Console 2026-07-12; TASKS.md entry corrected same day | chukwuemekanwoke-jpg | **Closed 2026-07-12** |
| R-04 | `ml-service` has no confirmed deploy mechanism at all | Operational | Medium-High | High | **Critical** | Cloud Build continuous-deploy trigger for ml-service (`rmgpgab-ml-service-...`, created 2026-07-08, branch `^develop$`) confirmed via `gcloud builds triggers list`/`describe` 2026-07-13 | chukwuemekanwoke-jpg | **Closed 2026-07-13** |
| R-05 | `origin/TeslatotheMars-patch-5`: stale base, deletes `RestaurantSetupView.jsx`+tests and the fastapi-app `Dockerfile` (which we just fixed today), bundles an unrelated 100k-row Egyptian delivery dataset | Operational | Low-Medium | High | High | Do not merge until Rui confirms intent — asked in group chat 2026-07-11 | Rui (clarify) / chukwuemekanwoke-jpg (gatekeep) | Open — escalated, awaiting reply |
| R-06 | Duplicated busyness-prediction effort: our heuristic area_factor vs. Rui's already-trained OLS/RF/XGBoost model, both targeting the same problem | Strategic | Medium | Medium | Medium-High | Coordination message sent 2026-07-11. Rui pushed his final model 2026-07-12 (`TeslatotheMars-patch-1`: `model training and evaluation_02.ipynb` + model description + final training data) — integration sync now actionable; maturation layer (2026-07-12) is model-agnostic so his model can slot in behind `/predict/busyness` | chukwuemekanwoke-jpg + Rui | Open — Rui delivered, sync needed |
| R-07 | `tabl_app` Cloud SQL password is weak (`ChangeMe1!`) and was pasted in plaintext in earlier chat-history debugging | Security | Low | Medium | Medium | Rotate to a random alphanumeric-only password (staging DB only, not prod) | chukwuemekanwoke-jpg | Open — not started |
| R-08 | `enrich-places.js` blocked on Places API (New) being enabled + real spend approval — easy to forget since it needs manual GCP Console action | Financial/Operational | Medium | Low | Low | Tracked in TASKS.md; enable + approve when convenient | chukwuemekanwoke-jpg | Open — low priority |
| R-09 | RevPASH: no schema, no route, no revenue data; depends on Yang Liu's unconfirmed SCRUM-302 status | Operational | High | Medium-High | High | Base schema/view/route built directly (migrations 006/007, `GET /revpash`, PR #52) without waiting on Yang Liu; this risk's premise no longer holds | chukwuemekanwoke-jpg | **Closed 2026-07-21** (retroactively — base work actually shipped 2026-07-13, register just hadn't been corrected until R-20 investigation surfaced it) |
| R-10 | `rolling_busyness_7d` still never computed/fed to `/predict/busyness` despite `availability_snapshots` now being written — a silent quality gap, not an outage | Operational | High | Low-Medium | Medium | Build a 7-day rolling-average aggregation query and wire it into the busyness client call | chukwuemekanwoke-jpg | Open — not started |
| R-11 | `GET /restaurants/nearby` (diner-facing) still shows static busyness while the merchant detail view is live — inconsistent numbers across surfaces | Reputational | High | Medium | Medium | Deliberately deferred pending a batched/cached design (avoid N+1 ml-service calls from a list endpoint) | chukwuemekanwoke-jpg | Open — design decision needed |
| R-12 | Local `main`/`develop` branches can silently drift far behind origin (found 32/35 commits behind on 2026-07-11) with no process to prevent recurrence | Operational | Medium-High | Low-Medium | Medium | Adopt a habit of `git fetch --all` at the start of any session before drawing conclusions from local branch state | chukwuemekanwoke-jpg | Open — informal awareness only |
| R-13 | `TASKS.md` "Done" entries can be inaccurate relative to actual repo state (see R-03) — undermines trust in the tracker as a status source | Operational | Medium | Medium | Medium | Spot-check other "Done" claims periodically; correct entries once verified | chukwuemekanwoke-jpg | Open — one instance found and corrected (R-03, 2026-07-12); keep spot-checking |
| R-14 | No confirmed Cloud SQL automated backup on `tabl-db-staging` | Security/Operational | Medium | High | **Critical** | Verify with `gcloud sql instances describe tabl-db-staging --format="value(settings.backupConfiguration)"`; enable via `gcloud sql instances patch` if absent — see `docs/rollback-recovery-runbook.md` | chukwuemekanwoke-jpg | Open — action assigned, not yet checked |
| R-15 | No verified fast-rollback mechanism; <2 min rollback SLA (TABL-507) not currently achievable via any confirmed path | Operational | Medium | High | High | Documented intended Cloud Run traffic-shift path in `docs/rollback-recovery-runbook.md`; R-04 (its blocker) is now closed, so this can be drilled and timed for real | chukwuemekanwoke-jpg | Open — unblocked, not yet drilled |
| R-16 | Booking-maturity blend (added 2026-07-12) reads booking timestamps with `EXTRACT(HOUR ...)` in the **DB server's timezone** while the gateway buckets predictions by its own local clock — if Cloud SQL and Cloud Run run in different timezones (typical default: UTC vs UTC, but unverified), same-hour bucket counts silently shift by the offset | Operational | Medium | Low-Medium | Medium | Verify both services' effective timezones; if they differ, pin the SQL extraction with `AT TIME ZONE` to match the gateway's clock | chukwuemekanwoke-jpg | Open — not started |
| R-17 | Onboarding busyness seeding makes `POST /restaurants` latency dependent on ml-service availability (adds stats query + ML round-trip inside the request); degradation is graceful (null → skip) but a slow-not-down ml-service delays merchant onboarding by up to `mlBusynessTimeoutMs` | Operational | Low-Medium | Low | Low | Acceptable for MVP scale; consider moving the seed prediction to a fire-and-forget/background pattern if onboarding latency ever matters | chukwuemekanwoke-jpg | Open — accepted for now |
| R-18 | `api-gateway`'s Cloud Run secrets (`DATABASE_URL`, `JWT_SECRET`, `GOOGLE_MAPS_API_KEY`) are wired to Secret Manager's mutable `latest` alias rather than pinned versions; disabling any version bumps `latest` to something Cloud Run can't inject, aborting all new instance startups | Operational/Security | Medium | High | **Critical** | `DATABASE_URL` re-pinned to explicit version 6 (2026-07-13, revision `api-gateway-00015-bzn`) as an immediate fix. Still needed: pin `JWT_SECRET` and `GOOGLE_MAPS_API_KEY` to explicit versions too, and decide a process for who's allowed to disable secret versions in use | chukwuemekanwoke-jpg | Open — partial fix applied, 2 of 3 secrets still exposed |
| R-21 | `api-gateway`'s Cloud Run env var for the Google Maps key was bound under the wrong name (`ROUTES_API_SECRET` instead of the `GOOGLE_MAPS_API_KEY` the code actually reads in `config.js`). Secret existed and was correctly attached, just under a name nothing read — so `config.googleMapsApiKey` was `null` in production and every ETA/distance call silently fell back to the local haversine estimate (`source: "estimate"`) instead of the real Google Routes API (`source: "google"`), since this trigger was created 2026-07-08. No error, no outage — a quietly degraded feature. | Operational | Medium (config drift from GCP-console setup, not repo-driven — same blind spot as R-03/R-04) | Medium — ETA accuracy silently degraded, not availability | Medium | Fixed via `gcloud run services update api-gateway --remove-secrets=ROUTES_API_SECRET --update-secrets=GOOGLE_MAPS_API_KEY=GOOGLE_MAPS_API_KEY:latest` (2026-07-13, revision `api-gateway-00016-wcx`) | chukwuemekanwoke-jpg | **Closed 2026-07-13** |
| R-19 | `RestaurantLocationPicker.jsx` (merged via PR #57) calls `GET /config/maps-key` to fetch the Google Maps browser key at runtime — this route doesn't exist on the backend. Degrades gracefully to manual lat/lng entry on the 404, so not user-blocking, but the map/autocomplete UX is dead until implemented. | Operational | High (route confirmed absent) | Low — graceful fallback exists | Low-Medium | Route implemented (PR #63), new browser-restricted key created + stored in Secret Manager as `MAPS_JS_API_KEY` (pinned to explicit version 1, not `latest`), bound to `api-gateway`. Promoted to `develop` (PR #65) and auto-deployed. Verified live 2026-07-21: `GET /config/maps-key` returns the real key, and `tabl-app-staging.web.app/register/restaurant` renders the real map picker (not the manual-entry fallback) | chukwuemekanwoke-jpg | **Closed 2026-07-21** |
| R-20 | `CampaignHistory.jsx`'s new RevPASH lift badge (merged via PR #57, SCRUM-309/TABL-215) calls `GET /restaurants/:id/campaigns/:campaignId/revpash-lift` — route doesn't exist, same root blocker as R-09 (RevPASH backend owned by Yang Liu). Degrades gracefully ("pending backend support (TABL-118)"), not user-blocking. | Operational | High (route confirmed absent) | Low — graceful fallback exists | Low-Medium | Implemented (PR #70): dealRevpash from campaign-linked bookings over the campaign's active window; organicRevpash from same-hour non-campaign bookings over a trailing 30-day baseline; liftPercent=0 when no baseline exists; offPeak vs. restaurant's median hourly revpash. No methodology spec existed anywhere in docs, so this PR defines and documents one (README/openapi/api-contract-v0.md) | chukwuemekanwoke-jpg | **Closed 2026-07-21** |
| R-23 | Every Tailwind color-opacity modifier (`bg-table-primary/10`, `border-table-danger/30`, `shadow-table-primary/25`, etc.) on the `table-*` theme palette silently resolves to fully transparent — confirmed via computed-style checks (`getComputedStyle` returns `rgba(0, 0, 0, 0)`), not a hunch. Root cause: `tailwind.config.js` defines these colors as raw `var(--table-primary, #31D5D5)` strings; Tailwind's opacity-modifier pipeline needs an unresolved `<alpha-value>` template (e.g. `rgb(49 213 213 / <alpha-value>)`) to inject an alpha channel, and silently drops the utility when it can't. Plain (non-opacity) color classes like `text-table-danger` are unaffected. This has been live since these tokens were introduced — every "tinted glass" status pill, alert-box background/border, and active-toggle tint across `BookingsList.jsx`, `CampaignHistory.jsx`, `LiveOfferTracker.jsx`, `AccessibilityPanel.jsx`, and the merchant dashboard's error banners has been rendering as fully invisible background/border (text/labels still show, since those use non-opacity classes) instead of the intended tint — a quietly degraded feature, no error, no outage, same shape as R-19/R-20/R-21. | Frontend/Operational | High (confirmed, currently live, spans most of the dashboard's status/alert UI) | Low-Medium — cosmetic only; text and functionality remain correct, just missing the intended tint/framing | Medium | Fixed app-wide (see Mitigated/Closed). | chukwuemekanwoke-jpg | **Closed 2026-07-23** |

## Mitigated / Closed

| ID | Description | Closed | Resolution |
|----|---|---|---|
| R-23 | Every Tailwind color-opacity modifier on the `table-*` theme palette silently resolved to fully transparent (`bg-table-primary/10`, `shadow-table-danger/30`, etc.) | 2026-07-23 | Added RGB-triple CSS custom properties (`--table-primary-rgb: 49 213 213`, one per token, for both the dark `:root` and `[data-theme='light']` blocks in `index.css`) alongside the existing hex vars, then repointed every `table.*` entry in `tailwind.config.js` at `rgb(var(--table-x-rgb, ...) / <alpha-value>)` so Tailwind can substitute the opacity modifier. Verified via computed-style checks in both themes (`bg-table-primary/10` → `rgba(49, 213, 213, 0.1)` dark, `rgba(8, 145, 178, 0.1)` light) and confirmed the fix cascades with zero code changes to pages never touched this session (`ForgotPasswordView.jsx`'s pre-existing `shadow-lg shadow-table-primary/10` now resolves correctly). Also converted the two inline `color-mix()` box-shadow workarounds added for R-23 (login + flash-deal buttons) back to plain `shadow-lg shadow-table-{color}/40` now that the utility works, so the workaround doesn't linger once its cause is gone. Full lint + 43/43 tests + production build all clean. |
| R-19 | `RestaurantLocationPicker.jsx` map picker dead — `GET /config/maps-key` route didn't exist | 2026-07-21 | Implemented the route (PR #63, `config.mapsJsApiKey`/`MAPS_JS_API_KEY`, 404 when unset). Created a new browser-restricted key (Maps JS + Places API only, HTTP-referrer-locked to `tabl-app-staging.web.app` + localhost dev) — deliberately separate from the server-side `googleMapsApiKey`, since a referrer restriction and server-to-server calls are mutually exclusive on one key. Stored as Secret Manager secret `MAPS_JS_API_KEY`, bound to `api-gateway` pinned to explicit version `1` (not `latest`, per the R-18 lesson). Promoted `integrate`→`develop` (PR #65); `api-gateway` auto-deployed. Verified live: endpoint returns the real key, and the registration page renders the actual map/autocomplete picker instead of the manual-entry fallback. |
| R-20 | `CampaignHistory.jsx`'s RevPASH lift badge dead — `GET /campaigns/:id/revpash-lift` route didn't exist | 2026-07-21 | Implemented (PR #70). Defined and documented a lift methodology from scratch (none existed anywhere in docs): dealRevpash from this campaign's bookings over its active window; organicRevpash from same-hour-of-day non-campaign bookings over a trailing 30-day baseline (not the campaign's own window, which usually has little organic activity by definition); liftPercent=0 instead of dividing by zero when no baseline exists; offPeak vs. the restaurant's median hourly revpash. 5 new tests (unit + route-level), OpenAPI/drift checks clean. |
| R-09 | RevPASH had zero backend progress, blocked on Yang Liu's unconfirmed SCRUM-302 status | 2026-07-13 (discovered stale 2026-07-21) | Actually resolved back on 2026-07-13 — migrations 006/007 and `GET /revpash` shipped via PR #52, built directly rather than waiting on Yang Liu. The register just never got corrected, so R-20 inherited a stale "blocked on Yang Liu" premise for over a week until the R-20 investigation caught it. Same failure mode as R-13 (stale tracker entries eroding trust) — worth a periodic spot-check habit, not just a one-off fix. |
| R-21 | `api-gateway`'s Cloud Run secret for the Google Maps key bound under the wrong env var name (`ROUTES_API_SECRET`), so ETA calls silently always used the haversine fallback instead of the real Routes API | 2026-07-13 | Confirmed via `gcloud run services describe` that the secret existed and was attached, just under a name `config.js` never reads. Fixed with `gcloud run services update api-gateway --remove-secrets=ROUTES_API_SECRET --update-secrets=GOOGLE_MAPS_API_KEY=GOOGLE_MAPS_API_KEY:latest` (revision `api-gateway-00016-wcx`). Verified live: `GET /api/v1/restaurants/:id/eta` now returns `"source":"google"` (was `"estimate"`). `JWT_SECRET`/`DATABASE_URL` untouched. Confirmed via the Cloud Build trigger's build config that deploys only patch the image, never env vars/secrets — so this fix persists across future `develop` auto-deploys. |
| R-03 | api-gateway GCP deploy trigger unverified from repo / TASKS.md claim mismatch | 2026-07-12 | Cloud Run continuous-deploy trigger (Cloud Build, from `develop`) confirmed in GCP Console; `deploy-staging.yml` comment documents the mechanism; TASKS.md Done entry corrected. Note: this confirmation covers **api-gateway only** — R-04 (ml-service deploy path) remains open and still gates R-15. |
| R-04 | ml-service (fastapi-app) deploy mechanism unconfirmed from repo search | 2026-07-13 | Same root cause as R-03: the trigger is GCP-console-configured, not repo YAML, so a repo-only search missed it. `gcloud builds triggers list` confirms `rmgpgab-ml-service-europe-west1-...` exists, is enabled, targets branch `^develop$`, and has been live since 2026-07-08 — it was already deploying ml-service the whole time this risk was open. Unblocks R-15. |

## Standup log

- **2026-07-11**: Register created from findings surfaced during busyness-prediction wiring work and branch review. 4 items flagged for escalation (R-01, R-02, R-04, R-09). Awaiting: Rui's reply (R-05, R-06), Yang Liu's status (R-09), GCP Console verification (R-03, R-04).
- **2026-07-11 (TABL-507)**: Wrote `docs/rollback-recovery-runbook.md`. Confirmed `git revert` itself is fast (~200ms, tested on a scratch branch) but found two new Critical/High risks in the process: no confirmed DB backup (R-14) and no verified fast-rollback path to actually hit the ticket's <2 min target (R-15). Both trace back to the same unresolved GCP Console verification as R-03/R-04 — escalating as one bundled ask rather than four separate ones.
- **2026-07-12 (R-09 checked, R-06 movement)**: R-09 re-verified against the repo — still zero RevPASH backend progress (no migration 006, no view, no route) and Yang Liu's only activity since 2026-07-09 is PR merges. Escalation trigger fires at Tuesday's standup as planned. Separately, Rui pushed his final busyness model today to `TeslatotheMars-patch-1` (notebook + description + training data), fulfilling his 2026-07-09 email commitment — R-06 moves from "waiting on reply" to "sync on integration." Caution: patch-1 was previously listed as a redundant branch to close without merging; that's now stale — it carries real new work and must be reviewed, not closed.
- **2026-07-12 (R-03 closed)**: Cloud Run continuous-deploy trigger for api-gateway confirmed directly in the GCP Console (Cloud Build, deploying from `develop` — matching the comment in `deploy-staging.yml`'s `build-backend` job). First register closure. TASKS.md's inaccurate "gcloud run deploy steps in deploy-staging.yml" Done entry corrected (R-13's known instance). Scope note: api-gateway only — the ml-service trigger (R-04) was NOT part of this confirmation and stays Critical/escalated.
- **2026-07-12**: Implemented merchant busyness maturation — new venues score from location prior (taxi + pedestrian) at onboarding, then blend toward their own confirmed bookings over 30 days (capped at 60% booking weight, shrinkage-damped). Two new risks logged: timezone consistency of the hour-bucket SQL (R-16, needs verification) and onboarding latency coupling to ml-service (R-17, accepted). Note R-06 (duplicated model effort with Rui) is still open — the maturation layer is model-agnostic (blends whatever the prior produces), so it survives a swap to Rui's trained model.
- **2026-07-13 (R-04 closed)**: While reviewing the `integrate` branch and its promotion path to `develop`, ran `gcloud builds triggers list` directly against `tabl-app-staging` and found `rmgpgab-ml-service-europe-west1-...` already exists, is enabled, and has targeted branch `^develop$` since 2026-07-08 — before this risk was even logged (2026-07-11). Same blind spot as R-03: these continuous-deploy triggers live in GCP Console config, not repo YAML, so the original "no visible trigger config anywhere in the repo" finding was accurate about the repo but wrong about the actual deploy mechanism. Closing R-04 and unblocking R-15 as a result.
- **2026-07-13 (R-18: staging outage from a disabled secret)**: Deployed staging site started 500-ing entirely (account creation was just the first symptom reported). Traced to `DATABASE_URL` secret version 7 — created earlier today by an unidentified party, then disabled — becoming the `latest` alias that Cloud Run's `api-gateway` pins to; Cloud Run cannot inject a disabled secret version, so every new instance failed to start as old ones autoscaled out. Fixed by re-pinning `DATABASE_URL` to explicit version 6 (known-good, used successfully all session for local Cloud SQL connector work); verified with a live registration request against the real staging URL (201 Created). Logged as R-18 rather than folding into R-07 (weak password) because the failure mode is structural — any Cloud Run secret pinned to `latest` is one `disable` action away from a full outage — and two more secrets (`JWT_SECRET`, `ROUTES_API_SECRET`) are still exposed to it. Who disabled version 7, and why, is still unknown.
- **2026-07-13 (R-21 closed: Routes API key silently unused in production)**: While investigating R-19 (missing `/config/maps-key` route), checked whether onboarding referenced the Maps key already stored in Secret Manager. Found the secret (`GOOGLE_MAPS_API_KEY`, created 2026-07-08) was correctly attached to `api-gateway`'s Cloud Run service — but bound under env var name `ROUTES_API_SECRET`, which nothing in the codebase reads (`config.js` only reads `process.env.GOOGLE_MAPS_API_KEY`). Result: every ETA calculation had been silently falling back to the local haversine estimate since this was deployed, with no error surfaced anywhere. Fixed by rebinding the secret under the correct name via `gcloud run services update` (revision `api-gateway-00016-wcx`); verified live that `/api/v1/restaurants/:id/eta` now returns `"source":"google"` instead of `"source":"estimate"`. Confirmed the fix will survive future deploys since the Cloud Build trigger's deploy step only patches the container image, never env vars/secrets.
- **2026-07-13 (PR #57 merged: frontend-integration-v2)**: Reviewed and merged Andrew Mitchell's `feature/frontend-integration-v2` into `integrate` (even with `integrate` already, no rebase needed). Brings password-reset frontend + shared `PasswordInput` component (backend support already on `integrate` via PR #55, so fully live now), a real server-side logout (`POST /auth/logout` + RTK Query cache reset), a `noValidate` fix for the campaign form's native-vs-styled validation conflict, plus two features built ahead of their backend: the RevPASH lift badge (`CampaignHistory.jsx`) and the Google Maps location picker (`RestaurantLocationPicker.jsx`). Both new frontend features degrade gracefully on 404 rather than breaking, but neither is functionally live yet — logged as new risks R-19 (`GET /config/maps-key` missing) and R-20 (`GET /restaurants/:id/campaigns/:campaignId/revpash-lift` missing, blocked on the same R-09 RevPASH backend work). Also checked a suspected `minLength={8}` regression on `RegisterView.jsx` from swapping in `PasswordInput` — false alarm, the form already has `noValidate` so the HTML attribute was inert; `isValidPassword` (JS, min 8 chars) still enforces it before submit. Local `integrate` fast-forwarded to `047cbdd` after the merge.
- **2026-07-21 (R-19 closed; R-22 opened)**: Implemented `GET /config/maps-key` (PR #63), created a new browser-restricted Maps JS/Places key separate from the server-side Routes API one (referrer restriction and server-to-server calls can't share a key), stored it as Secret Manager secret `MAPS_JS_API_KEY` pinned to explicit version `1`, bound it to `api-gateway`, and promoted `integrate`→`develop` (PR #65) so the auto-deploy trigger picked it up. Verified live: the endpoint returns the real key and the registration page renders the actual map picker. Closing R-19. While investigating a separate ask (check PR #64), found it's an unreviewed PR from Rui targeting `main` directly — technically low-risk content (only `ml-pipeline/notebooks/*`, no app code, CI green) but bypasses the promotion flow entirely on a `main` with zero branch protection (R-02), and sits there mergeable by anyone. Also found two commits (`d928c3b`, `17c843e`) pushed directly to `integrate` without a PR, breaking the reviewed-merge pattern this repo has followed since PR #50. Logged both as R-22; no action taken pending an explicit decision. Also, mid-session: a `git checkout develop` failed due to an unrelated uncommitted edit on `feature/frontend-sprint-4-complete` (present since before this session), but a subsequent `git pull origin develop` still ran against that branch instead, creating an unwanted local-only merge commit — caught immediately, undone with `git reset --merge ORIG_HEAD` (uncommitted edit preserved throughout), then redone safely by stashing the file first. No data lost, nothing was pushed.
- **2026-07-21 (R-20 closed; R-09 closed retroactively)**: Investigated R-20's premise before starting — found R-09 ("RevPASH blocked on Yang Liu") was stale: the base schema/view/route actually shipped 2026-07-13 via PR #52, built directly rather than waiting on Yang Liu, but the register was never corrected. So R-20 wasn't blocked on anyone external, just unbuilt. No lift-comparison methodology existed anywhere in `docs/`, so defined one before implementing: dealRevpash from this campaign's bookings over its active window; organicRevpash from same-hour-of-day non-campaign bookings over a trailing 30-day baseline (deliberately not the campaign's own window, since that usually has little organic activity by definition — that's often why a deal ran then); liftPercent=0 rather than a divide-by-zero when no baseline exists yet; offPeak vs. the restaurant's median hourly revpash. Implemented as `GET /campaigns/:id/revpash-lift` (PR #70, 5 new tests, OpenAPI/drift checks clean), merged into `integrate`. Closing both R-20 and R-09 (the latter closed retroactively to 2026-07-13, correcting the stale entry rather than leaving it as a live confusion source — same category as R-13's "stale tracker" concern).
- **2026-07-21 (promotion in flight)**: Opened PR #71 (`integrate`→`develop`, CI green, 7 commits) to carry both this session's closures — R-19 (`GET /config/maps-key`, PR #63) and R-20 (`GET /campaigns/:id/revpash-lift`, PR #70) — through to the deployed staging site, plus PR #69's campaign-offers endpoint. Net position after today: two features that were shipped-but-silently-dead in production (map picker, RevPASH lift badge) are now real; one stale risk (R-09) got corrected instead of left to mislead the next person; R-21's misnamed-secret bug from last week stayed fixed. Still open and untouched: R-22 (PR #64 / direct-push governance gap, awaiting a decision) and R-02 (main branch protection, made more urgent by R-22). PR #71 itself not yet merged — holding per usual pattern until explicitly told to merge.
- **2026-07-22 (PR #71 merged; frontend sprint 4 promoted via PR #72)**: PR #71 merged, closing out the previous entry's "held for merge" note — R-19/R-20/campaign-offers are now live on `develop`. Separately, `feature/frontend-sprint-4-complete` (PR #67) merged into `integrate`: Discover tab (top picks, quiet spots, cuisine browsing), cuisine photos on restaurant cards, editable profile preferences, a collapsible filters panel, shared search/busyness filtering across card and map views, an Ionicons refactor (replacing emoji icons across bookings/offers/inbox), a light-mode contrast fix on settings switches, and a fix for the map not tracking real GPS location after mount (`initialRegion` only applies once at mount; the camera now animates to the real fix via a ref once it lands asynchronously, instead of staying stuck on the Times Square fallback). That batch promoted `integrate`→`develop` via PR #72 and is now live on staging. No new risks surfaced by this batch. Still open and untouched: R-22 (PR #64 / direct-push governance gap) and R-02 (main branch protection).
- **2026-07-23 (login restyle + dashboard page split shipped; R-23 opened)**: Two new branches, each reviewed and CI-green before merging: `feature/login-restyle` (PR #82) reskins the login page to the "Kinetic Operational" theme (glowing `BrandMark` icon, ambient grid/glow backdrop replacing the old stock photo, glow under the primary button); `feature/dashboard-page-split` (PR #83) splits the single `/merchant` page — which fired every RTK Query hook on mount regardless of what was visible — into routed `/merchant` (Overview), `/merchant/tables`, `/merchant/settings` pages, each only calling the hooks it needs, plus a sidebar-nav shell rebuild and a `UserBadge` (initials avatar + the signed-in manager's real `displayName` from `GET /users/me`, not a placeholder). Both merged to `integrate`, then promoted `integrate`→`develop` via PR #84 (CI green) and merged. Deliberately left out of both: any UI the backend doesn't actually support (per-table floor plan, table/server search, team-invite controls, a system-health panel) — the Stitch design references we worked from assumed those, but no schema/route exists for any of them. While verifying the button glow rendered, found and logged **R-23**: every Tailwind color-opacity modifier (`bg-table-primary/10`, `shadow-table-primary/25`, etc.) on the `table-*` palette silently resolves to fully transparent, confirmed via computed-style checks — root cause is `tailwind.config.js` defining these colors as raw `var()` strings rather than an opacity-templated `rgb(... / <alpha-value>)` format. This has been live since the tokens were introduced and affects most of the dashboard's existing tinted status pills/alert boxes/toggle states, not just this session's new code. Worked around locally with inline `color-mix()` styles in the new components; app-wide fix not yet done — logged rather than fixed, since it's a much larger, separate change than what was asked for this session. Also noted in passing, not registered as a risk: local `backend/api-gateway` node_modules is currently missing `express-rate-limit` (blocks `npm run dev` locally), but CI's "API gateway (install + test)" job installs and passes cleanly, so this is a stale local install, not a manifest/repo issue — `npm install --workspace=backend/api-gateway` resolves it.
- **2026-07-23 (R-23 closed)**: Fixed the opacity bug app-wide on its own branch rather than patching individual components. Added RGB-triple CSS custom properties (`--table-primary-rgb`, etc., one per existing token) to both theme blocks in `index.css`, alongside the existing hex vars, then repointed every `table.*` color in `tailwind.config.js` at `rgb(var(--table-x-rgb, ...) / <alpha-value>)`. Two files, zero component changes needed for the fix itself to cascade — verified this directly by checking `ForgotPasswordView.jsx`'s pre-existing (untouched this session) `shadow-lg shadow-table-primary/10` now resolves to a real tinted shadow, and confirmed both dark and light theme variants resolve to the correct RGB triple via computed-style checks. Also reverted the two `color-mix()` inline-style workarounds from earlier today (login + flash-deal button glows) back to plain `shadow-lg shadow-table-{color}/40`, since the workaround's reason no longer exists. Full lint, 43/43 tests, and a production build all clean.
