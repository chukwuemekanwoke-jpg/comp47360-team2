# Copy to .env and adjust for local development:
#   cp .env.example .env

PORT=3001

# BE-2+ PostgreSQL (Docker: cd database && npm run db:up)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/table_dev

# BE-5+ (Google Distance Matrix) — uncomment when API key is available
# GOOGLE_MAPS_API_KEY=
