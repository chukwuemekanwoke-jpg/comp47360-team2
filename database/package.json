{
  "name": "table-database",
  "version": "1.0.0",
  "private": true,
  "description": "Tablé PostgreSQL migrations (BE-2)",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "migrate:down": "node scripts/migrate.js --down"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "pg": "^8.13.1"
  }
}
