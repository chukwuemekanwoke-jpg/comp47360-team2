# Dev-time API target — Expo loads this automatically on `expo start`.
# EAS builds ignore this file and use the env block in eas.json instead.
# To develop against a local backend on an Android emulator, use
# http://10.0.2.2:3001/api/v1 in a .env.local (gitignored) override.
EXPO_PUBLIC_API_URL=
